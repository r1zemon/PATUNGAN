"use client"

// This component is no longer used in the admin layout and can be considered removed.
// To fully remove it, you would delete this file and any references to it.
// For now, returning null ensures it renders nothing if accidentally imported.
export function TopNav() {
  return null;
}
